import NormalImg from '@components/Img/Img';

import styled from '@styles/styled-components';

const Img = styled(NormalImg)`
  height: 100%;
  max-height: 400px;
  margin-inline: auto;
`;

export default Img;
