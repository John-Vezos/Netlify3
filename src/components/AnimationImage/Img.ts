import styled from '@styles/styled-components';

import NormalImg from '../Img/Img';

const Img = styled(NormalImg)`
  width: 100%;
  margin: auto;
  aspect-ratio: 1 / 1;
  object-fit: contain;
`;

export default Img;
